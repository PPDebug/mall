spring:
  cloud:
    nacos:
      discovery:
        server-addr: http://${server-domain}:8848
      config:
        server-addr: http://${server-domain}:8848
        file-extension: yaml

server-domain: ${SERVER_DOMAIN:localhost}

logging:
  level:
    root: info